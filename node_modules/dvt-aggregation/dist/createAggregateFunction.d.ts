export interface IndicatorConfig {
    name: string;
    code: string;
    expression?: string;
}
export default function createAggregateFunction(indicators: IndicatorConfig[], { skipEmptyExpression }?: {
    skipEmptyExpression?: boolean;
}): (slice: any[]) => any;
