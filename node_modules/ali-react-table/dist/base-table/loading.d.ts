import { ReactNode } from 'react';
declare function Loading({ visible, children }: {
    visible: boolean;
    children: ReactNode;
}): JSX.Element;
declare namespace Loading {
    var defaultProps: {
        visible: boolean;
    };
}
export default Loading;
