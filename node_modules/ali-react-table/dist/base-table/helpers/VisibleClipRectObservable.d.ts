import { Observable } from 'rxjs';
interface SimpleDOMRect {
    readonly top: number;
    readonly right: number;
    readonly bottom: number;
    readonly left: number;
}
export declare function getClipRect(target: HTMLElement | typeof window, flowRoot: HTMLElement | typeof window): {
    offsetY: number;
    offsetX: number;
    clipRect: {
        left: number;
        top: number;
        right: number;
        bottom: number;
    };
};
/** 基于 ResizeObserver 和 scroll event 封装的 RxJS observable；
 * 用于监听一个元素的在页面中的「可见范围」的不断变化 */
export default class VisibleClipRectObservable extends Observable<{
    clipRect: SimpleDOMRect;
    offsetY: number;
}> {
    constructor(target: HTMLElement, flowRoot: HTMLElement | typeof window);
}
export {};
