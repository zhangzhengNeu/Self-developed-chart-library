import { VerticalRenderRange } from '../interfaces';
export declare const ITEM_SIZE = 40;
export default class ItemSizeStore {
    private cache;
    static getFullRenderRange(itemCount: number): VerticalRenderRange;
    getRenderRangeWhenBelowView(itemCount: number): VerticalRenderRange;
    getRenderRangeWhenAboveView(itemCount: number): VerticalRenderRange;
    getRenderRangeWhenInView(offset: number, maxRenderHeight: number, itemCount: number): VerticalRenderRange;
    private binarySearchStart;
    /** 获取虚拟滚动在 开始位置上的信息 */
    private getStart;
    private overscanUpwards;
    /** 获取虚拟滚动 在结束位置上的信息 */
    private getEnd;
    private overscanDownwards;
    private getItemSize;
    setMaxItemCount(itemCount: number): void;
    updateItem(index: number, offset: number, size: number): void;
    private getEstimatedTotalSize;
    private getMaxBottom;
}
