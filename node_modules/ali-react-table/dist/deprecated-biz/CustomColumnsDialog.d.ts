import { CSSProperties, ReactNode } from 'react';
import { ArtColumn } from '../interfaces';
/** @deprecated 表格操作栏与自定义列对话框 相关 API 将会在 1.0 版本被移除，请勿使用 */
export interface CustomColumnsDialogProps {
    defaultVisible?: boolean;
    trigger(onOpen: () => void): ReactNode;
    defaultCheckedCodes?: string[];
    enforceCheckedCodes?: string[];
    checkedCodes: string[];
    onChangeCheckedCodes(cols: string[]): void;
    columns: ArtColumn[];
    components: any;
    drawerProps?: any;
    style?: CSSProperties;
    className?: string;
    showCheckAll: boolean;
    showUncheckAll: boolean;
    drawerComponent?: any;
}
/** @deprecated 表格操作栏与自定义列对话框 相关 API 将会在 1.0 版本被移除，请勿使用 */
declare function CustomColumnsDialog({ trigger, defaultVisible, defaultCheckedCodes, enforceCheckedCodes, checkedCodes, onChangeCheckedCodes, columns, components, style, className, showCheckAll, showUncheckAll, drawerProps, drawerComponent: drawerComponentProp, }: CustomColumnsDialogProps): JSX.Element;
declare namespace CustomColumnsDialog {
    var defaultProps: {
        showCheckAll: boolean;
        showUncheckAll: boolean;
    };
    var makeStyledDrawer: (Drawer: any) => import("styled-components").StyledComponent<any, any, object, string | number | symbol>;
}
export default CustomColumnsDialog;
